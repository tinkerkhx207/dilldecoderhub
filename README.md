# http-client-6281dc

A Go HTTP client utility.

## Build & Run

```bash
go build -o client .
./client
```

## Environment

- `API_BASE` — base URL (default: `https://httpbin.org`)

## Project ID

`6281dc`
